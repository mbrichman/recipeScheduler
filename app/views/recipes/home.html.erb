<h1>My Recipe Box</h1>
<table>
  <th></th>
  <th>Recipe Name</th>
  <th>Source</th>
  <% @recipes.each do |recipe| %>
  <tr>
    <td><img src="<%= recipe.image_url %>" style="max-width: 100px"></td>
    <td><%= link_to recipe.name, recipe_path(recipe.id) %></td>
    <td><%= link_to recipe.source, recipe.source_url, target: '_blank' %></td>
  </tr>
  <% end %>
</table>


<!-- <div id="myCarousel" class="carousel slide">
  <ol class="carousel-indicators">
    <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
    <li data-target="#myCarousel" data-slide-to="1"></li>
    <li data-target="#myCarousel" data-slide-to="2"></li>
  </ol>
  <!-- Carousel items -->
<!--   <div class="carousel-inner">
    <div class="active item"><%= image_tag @recipes.first.image_url %>
      <div class='carousel-caption'><h4><%= @recipes.first.name %></h4></div>
    </div>
    <% @recipes.each do |recipe| %>
      <div class="item"><%= image_tag recipe.image_url %>
        <div class='carousel-caption'><h4><%= recipe.name %></h4></div>
      </div>
    <% end %>
  </div>
  <!-- Carousel nav -->
<!--   <a class="carousel-control left" href="#myCarousel" data-slide="prev">&lsaquo;</a>
  <a class="carousel-control right" href="#myCarousel" data-slide="next">&rsaquo;</a>
</div> -->
